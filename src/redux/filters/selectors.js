export const selectTransmission = state => state.filters.transmission;

export const selectEngine = state => state.filters.engine;

export const selectAC = state => state.filters.AC;

export const selectBathroom = state => state.filters.bathroom;

export const selectKitchen = state => state.filters.kitchen;

export const selectTV = state => state.filters.TV;

export const selectForm = state => state.filters.form;

export const selectCurrentTruck = state => state.filters.currentTruck;

export const selectReviewsLength = state => state.filters.reviewsLength;

export const selectFilter = state => state.filters.filter;

export const selectFavorite = state => state.filters.favorite;
